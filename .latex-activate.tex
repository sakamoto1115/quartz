% LaTeX Workshop の数式記号パネルを Markdown 編集中にも使うためのダミーファイル。
%
% LaTeX Workshop は登録済みの言語ID（latex, tex, bibtex など）のファイルを
% 開いたときにだけ起動し、`markdown` はその一覧に含まれていない。
% そのためこのファイルを一度開いてアクティビティバーに LaTeX アイコンを出すと、
% 以降は同じウィンドウ内で content/*.md を編集していても
% 記号パネルから \alpha などを挿入できる（挿入処理に言語IDの判定が無いため）。
%
% ビルドする必要は無い（.vscode/settings.json で自動ビルドを無効化している）。
